name: cron-job
mode: simple
description: "Scheduled Lambda function triggered by EventBridge"
cost_estimate: "$0/month on AWS free tier"
required_params:
  - project_name
optional_params: []
free_tier: true
audience: student
