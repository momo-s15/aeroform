name: lambda-api
mode: simple
description: "Lambda + API Gateway + DynamoDB backend"
cost_estimate: "$0/month on AWS free tier"
required_params:
  - project_name
optional_params:
  - custom_domain
free_tier: true
audience: student
